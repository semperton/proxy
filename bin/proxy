#!/usr/bin/env php
<?php

declare(strict_types=1);

use Semperton\Proxy\Proxy;

if (php_sapi_name() !== 'cli') {
	return;
}

require __DIR__ . '/../src/Proxy.php';

$proxy = new Proxy();

if (isset($argv[1])) {
	$proxy->setRequestUrl($argv[1]);
}

$proxy->execute(true);
